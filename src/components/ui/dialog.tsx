'use client';

/**
 * Minimal re-export of Radix Dialog primitives for the splash Modal component.
 * No styled wrappers needed — the Modal builds directly on primitives.
 */

import * as DialogPrimitive from '@radix-ui/react-dialog';

export { DialogPrimitive };
